<div class="flickr">
<H1>Recent photos:</H1>
	<ul>
		<% @photos.each do |photo|%>
			<li>
				<%= link_to image_tag(photo_url(photo.id), class: 'flick-photo',
															size: '200x200', title: photo.title), 
										user_photos(photo.owner)%>
			</li>
		<%end%>
	</ul>
</div>
